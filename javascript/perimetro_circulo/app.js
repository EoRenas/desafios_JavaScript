function areaEPerimetroCirculo(raio) {
    const pi = 3.14;
    const area = pi * raio * raio;
    const perimetro = 2 * pi * raio;
    console.log("Área:", area);
    console.log("Perímetro:", perimetro);
  }
  
  //exemplo
  areaEPerimetroCirculo(4);