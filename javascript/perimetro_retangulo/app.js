function areaEPerimetroRetangulo(largura, altura) {
    const area = largura * altura;
    const perimetro = 2 * (largura + altura);
    console.log("Área:", area);
    console.log("Perímetro:", perimetro);
  }
  
  //exemplo
  areaEPerimetroRetangulo(5, 3);